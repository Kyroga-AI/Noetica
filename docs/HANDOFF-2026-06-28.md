# Handoff — Sovereign AI / Workspace-One program (2026-06-28)

For the agent taking over. Read this first, then `MEMORY.md` in the memory dir. Everything below is real, committed code
unless flagged. **Test convention** (noetica): `cd agent-machine && node --import tsx --test lib/<x>.test.ts`;
`npm run typecheck`; `npx eslint`. Benchmarks in `model-registry.ts` are representative (~Jan-2026) — flagged, re-verify.

## 0. Repos in play (paths are absolute on this machine)
- `/Users/michaelheller/dev/noetica` — the cores live in `agent-machine/lib/`. **PUSH BLOCKED** (see §5).
- `/Users/michaelheller/dev/socioprophet` — `socioprophet-web/app-vue/` = Workspace/one UI + strategy docs. (pushes OK)
- `/Users/michaelheller/dev/prophet-platform` — `charts/prophet-workspace/` (Helm) + `apps/identity-prime/docs/`. (pushes OK)
- `/Users/michaelheller/dev/tritfabric` — `atlas/` = the Ray/MLOps substrate (Python). (pushes OK)
- `/Users/michaelheller/dev/prophet-core-contracts` — `contracts/gpu-catalog.v1.json` (canonical). (pushes OK)
- Commit identity for non-noetica pushes: `-c user.email="21163552+mdheller@users.noreply.github.com"` (gmail is blocked by GH).

## 1. Strategic thesis (the alignment)
**Vendor-neutral sovereign AI broker + workspace: consume everything (open weights + any cloud + any silicon),
correlate nothing, run anywhere, govern + attest.** Five composing moats: (1) cross-vendor + cross-silicon brokering
(incl. Huawei Ascend), (2) compulsion-resistant + anonymous identity, (3) Ray-portable sovereign MLOps, (4) graph-native
knowledge, (5) printable, tiered compliance. The "in front" line: nobody lets you pick origin/residency/egress/cloud/
silicon per tier with an attestation + a quantified %-of-frontier. **The choir is OPEN-WEIGHTS only — closed frontier
APIs (Claude/GPT/Gemini) are the measuring BAR, never fielded.**

## 2. Focus arc
- **Past 3 days:** Workspace/one product (mail/suite/identity keystone), cost analysis (cost-report.mjs), refresh
  framework + S0 CRDT sync (noetica/agent-machine/lib/graph-revision.ts, sync-engine.ts, sync-transport.ts), the
  prophet-workspace Helm chart.
- **Today (this session):** built the sovereign-identity keystone, the graph-native knowledge layer, the cross-vendor
  cloud broker (+neocloud +Asia +Ascend), unified the GPU catalog, the Ray/MLOps executor (in tritfabric), the
  two-class (open/regulated) choir + printable compliance + capability benchmarking, a 3-agent reassessment + hardening,
  and the 4-hyperscaler + Asia AI-stack survey.

## 3. What was built TODAY (file by file — all tested unless flagged)
**Sovereign identity keystone (`noetica/agent-machine/lib/`):**
- `sovereign-id.ts` (+test) — root seed → per-scope unlinkable Ed25519 facets + aliases (Senzing/MDM defeat).
- `sovereign-broker.ts` (+test) — passkey-style auth; root never leaves edge; **self-signed enroll** (impersonation fix).
- `sovereign-oidc.ts` (+test) — EdDSA OIDC issuance + JWKS (alg/iat/nonce checks hardened).
- `sovereign-vault.ts` (+test) — compulsion-resistant E2E seal under root-derived keys ("can't, not won't").
- `sovereign-broker-service.ts` (+test) — cloud verifier+issuer, holds only public material.
- `sovereign-broker-server.ts` + `sovereign-broker-main.ts` + `Dockerfile.broker` (+test) — runnable IdP (boots, serves OIDC).
- Arch doc: `prophet-platform/apps/identity-prime/docs/SOVEREIGN_ANONYMOUS_IDENTITY.md` (compulsion-resistance + DAO plan).
- Helm: `prophet-platform/charts/prophet-workspace/templates/broker.yaml` + values.

**Graph-native knowledge layer (the Notion leapfrog):**
- `noetica/agent-machine/lib/knowledge-graph.ts` (+test) — doc→graph projection + GDS (pagerank, pathBetween, backlinks, rollups).
- `knowledge-persist.ts` (+test) — sealed HellGraph write-path (structure clear for GDS, content vault-sealed).
- `choir-grounding.ts` (+test) — graph-grounded, scope-d-governed, anti-hallucination AI.
- UI: `socioprophet/socioprophet-web/app-vue/src/{services/knowledgeGraph.ts,services/knowledgeApi.ts,services/choirGrounding.ts,services/choirApi.ts,stores/knowledge.ts,views/Knowledge.vue}` + `KNOWLEDGE_LAYER.md`.

**Cloud broker + hyperscaler strategy:**
- `noetica/agent-machine/lib/cloud-broker.ts` (+test) — compute + services brokers; neoclouds + Alibaba + **Huawei Ascend (non-NVIDIA)**; loads canonical catalog. `cloud-provision.ts` (provCli map).
- `prophet-core-contracts/contracts/gpu-catalog.v1.json` — **canonical catalog, single source of truth** (both noetica + tritfabric load it; vendored copies at `noetica/agent-machine/lib/gpu-catalog.v1.json` and `tritfabric/atlas/gpu-catalog.v1.json` — re-vendor on change).
- UI: `socioprophet .../src/services/cloudBroker.ts`, `views/Cloud.vue`.
- Strategy docs (socioprophet app-vue): `HYPERSCALER_STRATEGY.md`, `AI_STACK_COMPARISON.md`.

**MLOps / Ray (tritfabric — the moat):**
- `tritfabric/atlas/gpu_broker.py` (+`tests/test_gpu_broker.py`) — cheapest-GPU broker → KubeRay RayCluster plan; loads canonical catalog.
- `tritfabric/atlas/ray_runner.py` — wired to attach the broker placement to the ledger. **NOTE: `_run_with_ray` is still a scaffold with a local fallback — the REAL Ray Train/LoRA trainer is NOT yet wired (verify/finish, see §4 WS-A).**
- (Removed the misplaced noetica `ray-broker.ts` — Ray belongs in tritfabric.)

**Two-class choir + compliance (`noetica/agent-machine/lib/`):**
- `choir-tier.ts` (+test) — OPEN (open-weights, any origin, no gate) + REGULATED: `US_GOV` (FedRAMP, Western-origin), `AU_GOV` (IRAP, Western-origin), `FINANCE` (SOC2/APRA, Western + CN-after-review). Origin gating; no-frontier-egress; in-boundary; audit.
- `model-registry.ts` — models with ORIGIN (US/EU/CN/frontier-api) + representative benchmarks + `pctOfFrontier`.
- `compliance-report.ts` (+test) — printable attestations (FedRAMP/NIST, IRAP/ISM, SOC2/CC, APRA CPS234, EU AI Act).
- `choir-bench.ts` (+test) — side-by-side capability: OPEN 97% (DeepSeek-R1), US/AU-Gov 89% (Llama-4), Finance 97%. **OPEN-WEIGHTS only; closed frontier = bar.**

**Workspace/one suite + marketplace + gitea + mail:**
- `socioprophet .../src/config/apps.ts` (suite registry), `views/{Mail,Market,Code,Cloud,Knowledge}.vue`.
- Docs: `MAIL_SPEC.md`, `WORKSPACE_ONE_GAP_ANALYSIS.md`, `WORKSPACE_ONE_AUDIT_2026H1.md`, `WORKSPACE_ONE_SCOREBOARD.md` (the living per-turn scoreboard), `DEVELOPER_PROGRAM.md`.
- `noetica/agent-machine/lib/mail-bridge.ts` + `mail-bridge-server.ts` (+tests) — IMAP↔REST, Hey model + Screener.
- `noetica/agent-machine/lib/marketplace.ts` (+test) — Linux-first, Flatpak-native (NOT LXC), permission-governed.
- `prophet-platform/charts/prophet-workspace/templates/gitea.yaml` + cloud-agnostic `_helpers.tpl`/`mail-lb.yaml` (cloudProvider value).

**Reassessment + hardening (from a 3-agent review):** fixed `loadOrCreateRoot` (ENOENT+wx, no root-loss), OIDC
alg/iat/nonce, broker self-signed enroll, HTTP body caps + timeouts, mail CORS+OPTIONS, mail thread-id collision,
`mergeGraphs` stub/dedupe, choir-error UX, Code iframe fallback, Market empty state.

## 4. Recommended path + next tranches
- **Phase 1 — PROVE ON METAL (gating priority).** Needs a Ray/GPU env (not present here).
  - **WS-A:** finish the real KubeRay submitter + the actual TritFabric LoRA trainer (`tritfabric/atlas/ray_runner.py::_run_with_ray` is a stub) → run a real fine-tune on broker-selected GPU.
  - **WS-C:** live neocloud burst + the "beats-frontier-on-your-tests, flat cost" client demo (noetica cloud-mesh-vs-frontier harness exists).
  - **Cost tee-up requested but NOT YET DONE** — itemize WS-A/WS-C run costs (GPU-hrs × broker rates from the canonical catalog) so it runs without surprises.
- **Phase 2 — Sellable:** SOC 2 Type II path (cheapest cert unlock) + a PUBLIC benchmark (choir vs frontier vs Granite Guardian) + wire `compliance-report` to real scope-d Event-IR.
- **Phase 3 — Product:** Workspace/one deploy P0s (build/push broker + mail-bridge images; Postgres-backed broker store; signing-key Secret in-chart; TLS non-optional; OIDC auth-code SSO for Gitea/Matrix) + UX P1s (knowledge editor render-in-place + autosave; mail compose/reply; one shared shell; a11y) + real Dovecot ImapStore adapter + ONLYOFFICE embed.
- **Phase 4 — Fuel:** compounding loop (fine-tune on tenant data → Ray Serve → verifier→selection tunes routing); Ray Data ingestion → graph; broker the cognitive gaps (vision/translate/doc-AI) rather than build.

## 5. Pre-identified gaps / risks (READ THIS)
1. **PROOF GAP (#1):** ~everything is proven-in-tests, NOT run on real infra. Convert before any customer demo.
2. **TritFabric trainer is a scaffold** — `_run_with_ray` falls back to local; the real LoRA trainer must be verified/built. (Memory `noetica-tritfabric-training-substrate.md` may overstate this.)
3. **noetica PUSH IS BLOCKED** — the repo's husky **pre-push hook is failing** (pre-existing, unrelated to these changes). **~8 noetica commits are local-only / unpushed.** First job: diagnose `noetica/.husky/pre-push`, fix the failing check (or get authorization to `--no-verify`), and land the commits. Other repos are pushed.
4. **Certs ≠ reports** — we can print FedRAMP/IRAP/SOC2 attestations; we hold no certifications.
5. **Benchmarks representative** — `model-registry.ts` numbers are ~Jan-2026 estimates; re-verify before client use.
6. **Suite is demo-grade** — Workspace/one apps are foundations/stubs (mail bridge has a server but uses a demo store; real Dovecot adapter pending).
7. **Cost tee-up for WS-A/WS-C not produced yet** (user asked; do it).

## 6. Conventions / gotchas
- noetica runs via `tsx` (resolveJsonModule on); JSON catalog is imported. Local-first; macOS-bias (Linux is the future-primary target).
- Don't spawn research agents for things answerable from knowledge (cost). The user is cost-sensitive — favor verifiable-in-env work; flag clearly when something can't be proven locally.
- Hot-swap vs release: frontend is a static export baked at release; backend sidecar hot-swaps. Don't claim a UI fix is live until released.
- Scoreboard: keep `WORKSPACE_ONE_SCOREBOARD.md` updated per turn (the user reviews it).
